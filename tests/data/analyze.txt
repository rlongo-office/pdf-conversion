xtract all coverages listed under lines of business from this attached text file; return only the lines of text that appears after 'SELECT LINES OF BUSINESS' and before 'ATTACHMENTS' and where the coverage text is preceded by a text line that has a single X; again, we only want coverage under line of business if and only if the line above it contains an 'X' (although the line containing the X could have whitespaces); stop extracting at the word 'ATTACHMENTS' and ignore everything beyond that; return only a list of the coverages as an array of JSON objects.